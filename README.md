# New manual install method
### Instructions:

#### Install
- cd into `manual_install`
- run `sudo bash install.sh`

#### When making changes to files restart the service
- `systemctl restart the-system`


